
# test_c18.mcsp -- testing

#THING (x,y) ::= @z.(x -> x -> y -> z)
#BLIP (z,y) ::= THING (z,y)

# SYSTEM (report) ::= (a -> SKIP) \ {a}

# SYSTEM (x,y) ::= (x -> SKIP) [] (y -> SKIP)

BASIC (x) ::= x -> SKIP

# TRIVIAL ::= ((a -> SKIP) || (a -> SKIP)) \ {a}

